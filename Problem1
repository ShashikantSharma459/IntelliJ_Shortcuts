class Solution {
    public int[] solution(int N) {
        for (int A = 1; A < N; A++) {
            int B = N - A;
            if (isValid(A) && isValid(B)) {
                return new int[]{A, B};
            }
        }
        // Fallback if no valid pair is found (should never occur given constraints)
        return new int[]{-1, -1};
    }

    // Helper method to check if number has any zero digit
    private boolean isValid(int num) {
        while (num > 0) {
            if (num % 10 == 0) return false;
            num /= 10;
        }
        return true;
    }
}
